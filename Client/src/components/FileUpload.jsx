// src/components/FileUpload.jsx

const FileUpload = () => {
  return (
    <div style={{ marginTop: '1rem' }}>
      <input type="file" />
      <button>Upload</button>
    </div>
  );
};

export default FileUpload;
